name: Trigger Kernel Build

on:
  push:
    branches:
      - master

permissions:
  actions: write
  contents: read

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger main kernel workflow
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run build.yml --ref master
